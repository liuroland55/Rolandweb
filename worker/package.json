{
  "name": "shiyu-worker",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute shiyu-db --local --file=./src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute shiyu-db --remote --file=./src/db/schema.sql",
    "db:migrate:local:0002": "wrangler d1 execute shiyu-db --local --file=./src/db/migrations/0002_user_profile.sql",
    "db:migrate:remote:0002": "wrangler d1 execute shiyu-db --remote --file=./src/db/migrations/0002_user_profile.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260908.1",
    "typescript": "^5.7.0",
    "wrangler": "^4.130.0"
  }
}
